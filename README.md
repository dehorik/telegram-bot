Для работы бота требуется в файле с именем ".env" указать токен. 
При запуске python-dotenv подгрузит переменные из файла ".env", pydantic их провалидирует и объект бота успешно создастся с нужным токеном.
